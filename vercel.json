{
  "buildCommand": "cd apps/web && pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "npm install -g pnpm@10.19.0 && pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}