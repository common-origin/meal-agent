name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          
      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 9
          
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        
      - name: Lint
        run: pnpm -C apps/web lint
        
      - name: TypeScript check
        run: pnpm -C apps/web typecheck
        
      # Note: Build disabled temporarily due to React version compatibility
      # - name: Build
      #   run: pnpm -C apps/web build
      
      # Future: Add accessibility testing
      # - name: A11y tests
      #   run: pnpm -C apps/web test:a11y